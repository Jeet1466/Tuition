{
  "name": "tproject",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" --names \"server,client\" --prefix-colors \"cyan,magenta\""
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
